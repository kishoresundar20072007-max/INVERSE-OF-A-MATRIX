# INVERSE-OF-A-MATRIX
## Aim:
To write a python program to find the inverse of a matrix
## Equipment’s required:
1. 	Hardware – PCs
2. 	Anaconda – Python 3.7 Installation / Moodle-Code Runner
## Algorithm:
### Step1 : Import the NumPy module to perform matrix operations.
### Step 2: Create the given matrix using np.array() and store it in a variable.
### Step 3: Use the np.linalg.inv() function to find the inverse of the matrix.
### Step 4: Display the inverse matrix and end the program

## Program:
```

import os
os.environ["OPENBLAS_NUM_THREADS"]="1"

import numpy as np

A = np.array([[2, 1, 1],
              [1, 1, 1],
              [1, -1, 2]])

solution = np.linalg.inv(A)

print(solution)      
```
## Output:
# INVERSE-OF-A-MATRIX
## Aim:
To write a python program to find the inverse of a matrix
## Equipment’s required:
1. 	Hardware – PCs
2. 	Anaconda – Python 3.7 Installation / Moodle-Code Runner
## Algorithm:
### Step1 : Import the NumPy module to perform matrix operations.
### Step 2: Create the given matrix using np.array() and store it in a variable.
### Step 3: Use the np.linalg.inv() function to find the inverse of the matrix.
### Step 4: Display the inverse matrix and end the program

## Program:
```

import os
os.environ["OPENBLAS_NUM_THREADS"]="1"

import numpy as np

A = np.array([[2, 1, 1],
              [1, 1, 1],
              [1, -1, 2]])

solution = np.linalg.inv(A)

print(solution)      
```
## Output:
width="1265" height="862" alt="Screenshot 2026-09-08 091609" src="https://github.com/user-attachments/assets/adb692e8-c1eb-47b4-8b3e-c51b75d27f69" />

## Result:
Thus the inverse of given matrix is successfully solved using python program



## Result:
Thus the inverse of given matrix is successfully solved using python program

